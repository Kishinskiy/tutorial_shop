apiVersion: v1
kind: Service
metadata:
  name: {{ include "tutorial-shop.fullname" . }}
  labels:
    {{- include "tutorial-shop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tutorial-shop.selectorLabels" . | nindent 4 }}
